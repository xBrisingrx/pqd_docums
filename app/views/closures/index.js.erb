$("#ticket_books").html("<%= j (render partial: 'index' ) %>")

$(document).ready(function(){
	closures_table = $("#closures_table").DataTable({
    'order': [[1, 'desc']],
    'ajax': `closures`,
    'columns': [
    {'data': 'name'},
    {'data': 'start_end_date'},
    {'data': 'tickets'},
    {'data': 'actions'}
    ],
    'language': {'url': datatables_lang}
	})
})

$("#ticket_books").modal('show')