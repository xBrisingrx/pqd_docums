$("#tickets_crud").html("<%= j (render partial: 'tickets/index' ) %>")

$(document).ready(function(){
  tickets_table = $("#tickets_table").DataTable({
    'ajax': `tickets?ticket_book_id=`+<%= @ticket_book_id %>,
    'columns': [
    {'data': 'number'},
    {'data': 'used'},
    {'data': 'active'}
    ],
    'language': {'url': datatables_lang}
  })

  // $("#form-disable-ticket-book").on("ajax:success", function(event) {
  //   ticket_books_table.ajax.reload(null,false)
  //   let msg = JSON.parse(event.detail[2].response)
  //   noty_alert(msg.status, msg.msg)
  //   $("#modal-disable-ticket-book").modal('hide')
  // }).on("ajax:error", function(event) {
  //   let msg = JSON.parse(event.detail[2].response)
  //   noty_alert("error", msg.msg.base[0])
  // })

})

$("#tickets_crud").modal('show')