<%= form_with(model: vehicle_location, local: true) do |form| %>
  <% if vehicle_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_location.errors.count, "error") %> prohibited this vehicle_location from being saved:</h2>

      <ul>
      <% vehicle_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
