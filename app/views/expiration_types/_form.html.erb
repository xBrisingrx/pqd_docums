<%= form_with(model: expiration_type, local: true) do |form| %>
  <% if expiration_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expiration_type.errors.count, "error") %> prohibited this expiration_type from being saved:</h2>

      <ul>
      <% expiration_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
